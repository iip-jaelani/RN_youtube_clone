const {default: Collection} = require('./Collection');
const {default: Home} = require('./Home');
const {default: Notifications} = require('./Notifications');
const {default: Subscription} = require('./Subscription');
const {default: Trending} = require('./Trending');

export const BottomNav = {
  Collection,
  Home,
  Notifications,
  Subscription,
  Trending,
};
